import React from 'react'

export default function NewsletterEmail() {
    return (
        <div>
            {/* return (
        <div>
            <h3>{comment.author_id}</h3>
            <p>{comment.body}</p>

        <div>
        <input
        value={newComment}
        onChange={e => setNewComment(e.target.value)}/>
        <button
        onClick = {() => props.updateComment(comment.commentId, body)}>Update
        </button>
        </div>

        <button
        onClick = {() => props.deleteComment(comment.commentId)}>Delete</button>
        </div>
    ) */}
        </div>
    )
}
